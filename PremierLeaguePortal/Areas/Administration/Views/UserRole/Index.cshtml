@model IEnumerable<PremierLeaguePortal.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<h3>Search</h3>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @*<div class="btn btn-success btn-block">
                    @Html.ActionLink("Assign author right", "AssignAuthorRole", new { id = item.Id }, new { @class = "plp-actionlink" })
                </div>*@
                @Html.ActionLink("Assign author right", "AssignAuthorRole", new { id = item.Id }, new { @class = "plp-actionlink btn btn-success btn-block" })
                @*<div class="btn btn-warning btn-block">
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "plp-actionlink" })
                </div>*@
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "plp-actionlink btn btn-warning btn-block" })
            </td>
        </tr>
    }

</table>
